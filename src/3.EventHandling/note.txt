리액트 이벤트 규칙
    1. 이벤트 이름은 카멜 케이스로 작성 onClick
    2. 이벤트에 실행할 자바스크립트 코드를 전달하는 것이 아니라, 함수 형태의 값을 전달하는 것이다.
        => 함수 형태의 객체 전달
    3. DOM 요소에만 이벤트를 설정할 수 있다.
        => div, span 등 DOM 요소에 이벤트를 붙이는 것은 가능하나, 컴포넌트 자체에 이벤트를 생성하는 것은 불가능
        => 이는 단순히 지정한 이름에 해당하는 props를 전달하는 것 밖에 안된다.


